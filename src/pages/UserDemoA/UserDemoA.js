import React from 'react';
import './UserDemoA.scss';
import PropTypes from 'prop-types';

const UserDemoA = (props) => {
  return <div className="UserDemoA">UserDemoA</div>;
};

UserDemoA.propTypes = {};

export default UserDemoA;
