import React from 'react';
import './Store.scss';
import PropTypes from 'prop-types';

const Store = (props) => {
  return (
    <div className="store">
      <h2>Store</h2>
    </div>
  );
};

Store.propTypes = {};

export default Store;
