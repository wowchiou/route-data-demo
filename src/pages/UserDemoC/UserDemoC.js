import React from 'react';
import './UserDemoC.scss';
import PropTypes from 'prop-types';

const UserDemoC = (props) => {
  return <div className="UserDemoC">UserDemoC</div>;
};

UserDemoC.propTypes = {};

export default UserDemoC;
